<% cache "p/i/#{params}/#{@cache_id}" do %>
  <ul>
  <%= render collection %>
</ul>
<% end if collection %>
<div class="c"></div>
<div class="r">
  <%= will_paginate collection if collection %> 
</div>
<%#= link_to t('labels.new'), new_price_path %>
