<%= form_for(@my_task) do |f| %>
  <% if @my_task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@my_task.errors.count, "error") %> prohibited this my_task from being saved:</h2>

      <ul>
      <% @my_task.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
