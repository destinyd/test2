<%= render 'before_layout' %>
    <article>
      <% flash.each do |name, msg| %>
        <%= content_tag :div, msg, :id => "flash_#{name}" %>
      <% end %>
      <%= yield %>
    </article>
<%= render 'after_layout' %>
